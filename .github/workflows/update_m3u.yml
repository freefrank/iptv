name: Update M3U Playlist

on:
  schedule:
    - cron: '0 0 * * *'  # 每天执行一次
  push:
    branches:
      - main

jobs:
  update-m3u:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: pip install requests

    - name: Run the script
      run: python script.py

    - name: List files for debug
      run: ls -al

    - name: Commit changes
      env:
        ACTIONS_PAT: ${{ secrets.ACTIONS_PAT }}
      run: |
        git config --global user.name 'freefrank'
        git config --global user.email 'freefrank21@gmail.com'
        git remote set-url origin https://${{ github.actor }}:${{ secrets.ACTIONS_PAT }}@github.com/${{ github.repository }}.git
        git add processed_playlist.m3u
        git commit -m 'Update processed_playlist.m3u'
        git push origin main
